# ④ 잔여 D·F·G·H 크기 — 재측정 (레인 C, 2026-08-10)

★조회. 코드 0줄. 앱 `05a65c5` / 워커 `5257120` 기준.

전제: `lane_c_item4_sizing_2026-08-08.md`가 이미 있다(확인함, 다시 안 만듦) --
D·H·F의 기술 전제는 오늘 재확인했고 안 바뀌었다(아래 표). **바뀐 건 G뿐이다** --
G는 사실상 완료됐고, 남은 두 항목의 성격이 08-08엔 "미노출"이었는데 오늘은
**둘 다 구조적으로 막혀 있다**(아래).

★★**후속 정정(같은 날 저녁) -- D의 전제가 바뀌었다.** 아래 "D: 워커 스파이크
선행" 행은 이 문서를 쓴 시점엔 참이었지만, 그 뒤 실제로 스파이크를 돌렸다
(`reports/lane_c_item4_d_keyframe_worker_spike_2026-08-10.md`) -- `sendcmd`
없이 `eval=frame` + `t` 표현식만으로 값 곡선이 실측 확인됐다. **D는 이제
워커 스파이크가 아니라 결정①②(무엇을 키프레임하는가, 보간 어휘)만 남았다.**
아래 표·권고의 "D=워커 스파이크"는 그 정정 전 스냅샷으로 읽을 것 -- 최신
상태는 D 스파이크 리포트 + `lane_c_state_2026-08-10.md`.

## 결론 먼저

**날짜 아님, 크기만.** R2가 검증 대상으로 잡을 수 있는 건 지금 **G의 두 잔여
(lutIntensity·motionBlur)** + **D**(스파이크 해소, 설계 착수 가능)다 -- H·F는
여전히 **선행 조건 미해소**(H=제니2 결정, F=계약).

## D·H·F -- 기술 전제 재확인 (그렙, 오늘)

| 주장 (08-08) | 오늘 재확인 | 결과 |
|---|---|---|
| D: 워커에 `sendcmd` 없음 | `grep sendcmd src/*.ts` → 0건 | ★**여전히 참** |
| D: `setpts`/`enable=between(t,…)` 있음(켜기·끄기, 값 곡선 아님) | 둘 다 존재 확인(`render.ts:76,397`) | ★**여전히 참** |
| D: `lib/edl-keyframes.ts` 없음 | 파일 부재 확인 | ★**여전히 참** |
| H: `totalMs = Σ(endMs−startMs)`, speed 미반영 | `ProComposeEditor.tsx:730`, `lib/studio.ts:1561` 둘 다 확인 | ★**여전히 참** |
| F: TTS 관련 파일 0 | 양 레포 `*tts*` 검색 0건 | ★**여전히 참** |

**바뀐 것 없음 -- D·H·F는 08-08 수치·순서 그대로 유효하다.**

## G -- 완료분 실측 (오늘 커밋, 추정 아님)

| | 파일 | 줄 | 결정 | 비고 |
|---|---|---|---|---|
| sharpen 노출 | 2(`lib/effects.ts`, harness 주석) | +34/−15 | 0(★재구성 때 이미 지불) | 셰이더는 어제 배선 끝 |
| chromatic 전체(셰이더+배선+노출) | 4 | +93/−16 | 1(방향, ffmpeg 실측으로 해소) | 새 포트 하나의 **실제 원가** |

★**08-08의 "효과당 1개 + 공통 1개" 결정 예측이 맞았다** -- 공통 결정(저크기 게이트
형태)은 어제(08-09→08-10 재구성)에 이미 지불됐고, 오늘은 효과당 결정만 남아
sharpen=0(재사용), chromatic=1(방향)이었다.

## G 잔여 -- 이제 2개, 둘 다 "미노출"이 아니라 "구조적으로 막힘"

| | 파일(추정) | 줄(추정) | 결정 | 막힌 이유 |
|---|---|---|---|---|
| **lutIntensity** | 4(워커 `render.ts`, 앱 `gl-effects.ts`·`effects.ts`, 파리티 케이스) | ~20-30(참고: `buildSegmentFC` 기존 29줄짜리 함수 확장 가능성 높음) | **1** | ★워커가 LUT를 **아예 전강도로만** 적용한다(`effectVideoFilters` 확인, blend 없음) -- intensity 슬라이더가 지금 프리뷰에서만 존재해도 **워커에 대응하는 게 없다.** 결정 = ffmpeg에서 부분강도 블렌드를 어떻게 내는가(glow의 `blend=all_opacity=`와 같은 필터체인 재구성이 필요할 가능성 -- **실측 필요, 오늘 안 함**) |
| **motionBlur** | 2+(`preview-gl.ts`에 새 구조, `gl-effects.ts`) | ★**스파이크 전엔 못 낸다** | **2+** | `tmix`는 **여러 비디오 프레임**을 평균한다 -- 지금 프리뷰 엔진은 프레임 하나 들어와서 하나 나가는 구조라 **프레임 히스토리 버퍼가 없다.** 셰이더 포트가 아니라 **새 능력 클래스** -- D가 워커 스파이크가 먼저듯, 이것도 GL 프리뷰 스파이크(프레임 N장 버퍼링이 이 엔진 구조에서 되는지)가 먼저다 |

## 권고 (★결정은 제니2, 08-08과 동일)

1. **lutIntensity**부터 -- 결정 1개(워커 블렌드 방식)만 나오면 파일 수는 작다
   (기존 `buildSegmentFC`를 확장하는 모양일 가능성이 커서, glow만큼 크지 않을
   것으로 보임 -- ★이것도 실측 아닌 추정이라 스파이크 전엔 확정 아님).
2. **motionBlur**는 D처럼 스파이크 먼저 -- "프레임 히스토리 버퍼가 이 엔진에서
   되는가"가 결정①보다 앞선 사전 질문이다.
3. D·H·F는 08-08 순서 그대로: D(워커 스파이크) → H(제니2 결정) → F(계약).

★**R2 스코프에 주는 답**: 지금 코드 0줄로 바로 검증 가능한 건 없다 -- G의 둘,
D, H, F 전부 각자의 선행 질문이 안 풀렸다. R2가 "무엇을 검증 대상으로 삼을지"는
그 선행 질문들 중 어느 것이 먼저 풀리느냐에 달려 있다.
