# vignette 수학 불일치 -- 조사 결과 (2026-08-10, 지수2C)

작업 공간: `C:\Users\Tom\oxxovo-lane-c`, `feat/studio-lane-c`. 도구:
`scripts/probe-vignette-model.mjs`, `scripts/fit-vignette-model.mjs` (둘 다 커밋).

## 결론 먼저

★★**정확한 폐쇄형 수식을 못 냈다.** 체계적으로 시도했고(1절), 슬라이더가 실제로
만드는 각도 범위(`PI/6`~`PI/2`) 전체에서 1% 이내로 맞는 후보가 하나도 없었다
(2절 표). ★**임의로 근사식을 넣지 않았다** -- 몇 % 틀린 채로 "고쳤다"고 하는 것은
지금 셰이더(19.53% 틀림)보다 조금 나은 정도일 뿐, 참가자가 보는 미리보기와 실제
렌더가 다르다는 문제 자체를 안 없앤다. **다음 단계는 폐쇄형 공식이 아니라 실측
룩업(LUT) 기반**이라고 판단한다 -- 이건 새 설계 결정이라 오늘 만들지 않았다
(3절, ★판정 필요 지점).

## 1. 확실히 알아낸 것 -- 대칭성

`probe-vignette-model.mjs`: 중심에서 **같은 픽셀 거리**, 다른 방향(수평 vs
45도 대각선) 두 점을 비교 -- **감쇠율이 소수점 4자리까지 정확히 일치**(0.8516).
★이게 뜻하는 것: 감쇠는 **원본 픽셀 거리**만의 함수다(가로/세로 따로 정규화한
타원형이 아니다, `aspect` 파라미터는 이 케이스에 안 걸림). 이 하나는 재측정
없이 믿어도 된다 -- 방향이 다른데 값이 자리까지 같은 건 우연이 아니다.

## 2. 폐쇄형 후보 -- 전부 시도, 전부 tolerance 밖

`fit-vignette-model.mjs`가 순서대로:
1. **물리적으로 그럴듯한 후보**(렌즈각 `cos^4`, 초점거리 기하 등)를 각도값 그대로
   대입 -- 전부 4~8% 틀림.
2. **자유 1파라미터 피팅**(k를 데이터가 고르게) -- `cos^4(atan(k*d))`가 최선,
   기본각(PI/5)에서 1.37%까지 내려감. 여전히 tolerance(1%) 밖.
3. **슬라이더가 실제로 만드는 각도 범위 전체**(`render.ts`: `angle = PI/(6-vg/25)`,
   vg 0~100 → PI/6~PI/2)에서 재검증 -- **다섯 지점 전부 tolerance 밖**, 각도가
   커질수록(참가자가 슬라이더를 세게 쓸수록) 더 나빠진다(vg=100에서 7.48%).
4. **k와 코사인 지수 p를 둘 다 자유롭게**(p까지 12까지 열어서) 가장 어려운
   지점(vg=100)에서 다시 피팅 -- 그래도 4.97%. ★p가 탐색 상한(12)에 붙었다는
   것 자체가 신호 -- `cos^p` 계열 자체가 이 지점에서 안 맞는다는 뜻이지, 지수를
   더 키운다고 해결될 문제가 아닐 가능성이 크다.

## 3. ★판정 필요 -- 다음 단계 제안

**폐쇄형을 더 찾는 대신 실측 데이터 기반(룩업)으로 가는 걸 제안한다**: ffmpeg의
실제 출력을 텍스처로 굽고(각도별 반경 곡선), GL 셰이더가 그 텍스처를 참조.
★★이건 오늘 범위를 넘는 새 설계다 -- 텍스처 포맷·해상도(반경 1차원이면 충분한지,
각도별로 몇 장 굽을지)·어디서 굽는지(빌드타임 vs 요청시 ffmpeg 호출) 전부
결정이 필요하고, 잘못 잡으면 다시 부정확한 근사가 된다. **승인 있으면 다음
착수로 잡겠다.**

## 4. grain 순서 재확인 -- 얽혀 있지 않다 (논리로 확인, 재측정 대기)

Q3(08-10, `lane_c_item4_glow_combo_parity_2026-08-10.md`)의 "재배열이 vignette
단독만큼 움직인다" 비교는 **GL 대 GL**이다 -- old-order와 shipped-order 양쪽 다
**같은(지금은 틀린) vignette 셰이더**를 쓴다. vignette 공식 자체가 ffmpeg와
안 맞아도, **그 셰이더를 grain보다 먼저/나중에 두는 것의 차이**는 그 셰이더가
무엇이든 자기 자신과 비교하는 것이라 독립적으로 유효하다 -- 얽혀 있지 않다.
★단, **vignette 셰이더를 실제로 고친 뒤에는 Q3를 다시 돌려서 비율 숫자를
갱신해야 한다**(질적 결론 "재배열이 진짜다"는 안 바뀔 것으로 보이지만, 정확한
비율은 새 셰이더로 다시 재는 게 맞다) -- 오늘은 안 함(vignette 자체가 아직 고쳐지지
않아서 잴 대상이 없다).

## 5. 검증

`node scripts/probe-vignette-model.mjs` / `node scripts/fit-vignette-model.mjs`
둘 다 쓰기 0, 읽기(ffmpeg 호출)만. tsc/npm test 영향 없음(신규 .mjs 스크립트,
기존 코드 변경 없음 -- **셰이더는 오늘 안 건드렸다**, 조사만).
