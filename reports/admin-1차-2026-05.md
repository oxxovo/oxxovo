# OXXOVO Admin 1차 작업 보고

**작성일**: 2026-05-23
**작성자**: 지수 (Claude Opus 4.7)
**상태**: TK 대표님 SQL 실행 대기 → 실행 확인 후 코드 작업 진행

---

## ⚠️ 솔직 모드 — 응답 잘림 알림

대표님 직전 응답에서 **"큰 추가 — admin 페이지 운영 데이터 관리"** 4가지 영역 중 **1, 2번만 보이고 3, 4번이 잘림** (메시지가 8번 반복되며 truncated). 1차 마이그레이션 SQL은 인증 시스템만 다루므로 영향 없이 진행 가능하지만, **2차 작업 계획 업데이트 전 3, 4번 영역 디테일 재확인 필요**.

확인된 1, 2번:
1. **라운드별 참가자 명단** — 신청자(전체)/Top 50/Waitlist/본선 진출자/시상자 + 필터/검색/CSV Export
2. **수상자 명단 + 점수별 등급** — 95+ LEGENDARY / 90+ MASTERPIECE / 80+ EXCELLENT (Top 1%, Top 10%)

3, 4번 디테일 재요청 → 받은 후 admin-plan-2026-05.md 업데이트.

---

## 사전 정리 진행 상황

### A. `formatPanelLabel` 통일 — ✅ 완료

- `app/page.tsx`: `tripleOr` local helper 제거, 5곳 치환 + import 추가
- tsc 통과, `tripleOr` 코드 grep 0건
- **commit 대기** (B SQL 실행 후 함께 commit 권장)

### B. `season.name` 분리 — SQL 실행 대기

TK 대표님이 Supabase Dashboard SQL Editor에서 실행:

```sql
-- 사전 정리 B: season.name "GENESIS Season 0" → "GENESIS"
-- season_number 컬럼은 이미 0으로 존재
UPDATE seasons
SET name = 'GENESIS', updated_at = NOW()
WHERE season_number = 0;

-- 검증 query (실행 후 확인용)
SELECT id, name, season_number FROM seasons WHERE season_number = 0;
-- 결과: name = 'GENESIS', season_number = 0
```

---

## 1차 작업 — 인증 시스템 마이그레이션 SQL

TK 대표님 Supabase Dashboard SQL Editor에서 순서대로 실행.

### Step 1 — `profiles` 테이블 + 트리거

```sql
-- profiles 테이블 (admin role 관리)
CREATE TABLE IF NOT EXISTS profiles (
  id UUID PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE,
  email TEXT NOT NULL UNIQUE,
  role TEXT NOT NULL DEFAULT 'user' CHECK (role IN ('user', 'admin')),
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- updated_at 자동 업데이트
CREATE OR REPLACE FUNCTION set_updated_at()
RETURNS TRIGGER AS $$
BEGIN
  NEW.updated_at = NOW();
  RETURN NEW;
END;
$$ LANGUAGE plpgsql;

DROP TRIGGER IF EXISTS profiles_set_updated_at ON profiles;
CREATE TRIGGER profiles_set_updated_at
  BEFORE UPDATE ON profiles
  FOR EACH ROW EXECUTE FUNCTION set_updated_at();
```

### Step 2 — `is_admin()` RPC

```sql
-- RLS 정책에서 사용할 admin 체크 함수
CREATE OR REPLACE FUNCTION is_admin()
RETURNS BOOLEAN
LANGUAGE sql
STABLE
SECURITY DEFINER
SET search_path = public
AS $$
  SELECT EXISTS (
    SELECT 1 FROM profiles
    WHERE id = auth.uid() AND role = 'admin'
  );
$$;
```

### Step 3 — RLS 정책

```sql
-- profiles RLS
ALTER TABLE profiles ENABLE ROW LEVEL SECURITY;

DROP POLICY IF EXISTS profiles_own_read ON profiles;
CREATE POLICY profiles_own_read ON profiles
  FOR SELECT USING (id = auth.uid());

DROP POLICY IF EXISTS profiles_admin_all ON profiles;
CREATE POLICY profiles_admin_all ON profiles
  FOR ALL USING (is_admin());

-- seasons RLS (read는 누구나, write는 admin만)
ALTER TABLE seasons ENABLE ROW LEVEL SECURITY;

DROP POLICY IF EXISTS seasons_read_all ON seasons;
CREATE POLICY seasons_read_all ON seasons
  FOR SELECT USING (true);

DROP POLICY IF EXISTS seasons_admin_write ON seasons;
CREATE POLICY seasons_admin_write ON seasons
  FOR INSERT WITH CHECK (is_admin());

DROP POLICY IF EXISTS seasons_admin_update ON seasons;
CREATE POLICY seasons_admin_update ON seasons
  FOR UPDATE USING (is_admin());

DROP POLICY IF EXISTS seasons_admin_delete ON seasons;
CREATE POLICY seasons_admin_delete ON seasons
  FOR DELETE USING (is_admin());
```

### Step 4 — TK 대표님 admin 등록

먼저 Supabase Dashboard → Authentication → Users 탭에서 본인 회원가입:
- Add user → Send invitation
- 이메일: `tkckusa@gmail.com`
- 이메일 도착하면 비밀번호 설정

회원가입 완료 후 SQL Editor에서 실행:

```sql
-- TK 대표님 admin role 부여
INSERT INTO profiles (id, email, role)
SELECT id, email, 'admin'
FROM auth.users
WHERE email = 'tkckusa@gmail.com'
ON CONFLICT (id) DO UPDATE SET role = 'admin', updated_at = NOW();

-- 검증
SELECT p.email, p.role, p.created_at
FROM profiles p
WHERE p.role = 'admin';
-- 결과: tkckusa@gmail.com / admin
```

### Step 5 — 검증 query (실행 후 확인)

```sql
-- profiles 테이블 존재 + RLS 활성화 확인
SELECT tablename, rowsecurity FROM pg_tables WHERE tablename IN ('profiles', 'seasons');
-- 결과: 두 행 모두 rowsecurity = true

-- is_admin() 함수 존재
SELECT proname FROM pg_proc WHERE proname = 'is_admin';
-- 결과: is_admin

-- RLS 정책 카운트
SELECT tablename, COUNT(*) as policy_count
FROM pg_policies
WHERE tablename IN ('profiles', 'seasons')
GROUP BY tablename;
-- 결과: profiles 2, seasons 4
```

---

## TK 대표님 실행 순서 요약

1. **사전 정리 B**: `UPDATE seasons SET name = 'GENESIS'` 실행
2. **1차 Step 1~3**: profiles + is_admin + RLS 마이그레이션
3. **1차 Step 4 사전**: Supabase Auth Dashboard에서 본인 회원가입 (`tkckusa@gmail.com`)
4. **1차 Step 4**: admin role INSERT
5. **1차 Step 5**: 검증 query 실행 후 결과 지수에게 공유

→ 모두 완료되면 코드 작업 진행 (인증 미들웨어, /admin 대시보드, /admin/seasons CRUD).

---

## 코드 작업 계획 (SQL 실행 후 진행)

### 의존성

```bash
# Supabase Auth helpers (httpOnly cookie 기반 SSR)
npm install @supabase/ssr
```

### 새 파일 (예상)

```
middleware.ts                              # admin route 보호
app/admin/layout.tsx                       # admin 공통 레이아웃 (사이드바)
app/admin/page.tsx                         # 대시보드
app/admin/login/page.tsx                   # admin 로그인 (Supabase Auth)
app/admin/seasons/page.tsx                 # 시즌 목록
app/admin/seasons/new/page.tsx             # 새 시즌 생성
app/admin/seasons/[id]/page.tsx            # 시즌 수정
lib/supabase-server.ts                     # server-side Supabase client (cookie)
lib/supabase-browser.ts                    # client-side Supabase client
lib/admin-auth.ts                          # admin 체크 헬퍼 (server-side)
lib/season-schema.ts                       # zod 검증 스키마
```

### 시각 — admin 사이드바

- 메인 사이트: 보라색 (`#8b22ff`) + 검정
- admin: **빨간/주황 톤 액센트** (대표님 결정 4번) — 예: `#ff4444` 또는 `#ff8800` 사이드바 액센트
- admin 모드 시각 구분 (모드 잘못 알아 위험한 액션 방지)

### Env 변수 (Vercel 추가 필요 — TK 대표님 직접)

- `NEXT_PUBLIC_SUPABASE_URL` (이미 있을 듯)
- `NEXT_PUBLIC_SUPABASE_ANON_KEY` (이미 있을 듯)
- `SUPABASE_SERVICE_ROLE_KEY` — **신규 추가 필요** (server-side admin 작업용, RLS 우회)
- `RESEND_API_KEY` — 이미 있음 (옥소보 5 확정)

→ Vercel Dashboard → Settings → Environment Variables 에서 `SUPABASE_SERVICE_ROLE_KEY` 추가.
   값은 Supabase Dashboard → Project Settings → API → `service_role` (secret) 키 복사.

---

## 검증 계획 (코드 작업 후)

- tsc --noEmit 통과
- `/admin` 미인증 접근 → `/admin/login` 리다이렉트
- TK 대표님 로그인 → `/admin` 진입 가능
- 비admin user 로그인 → `/admin` 차단 (403 또는 리다이렉트)
- `/admin/seasons` 목록 표시 (현재 season 1개)
- `/admin/seasons/[id]` 수정 폼 동작 + zod 검증
- DB 변경 → /, /rules, /apply 페이지 즉시 반영 확인

---

## 다음 단계

1. **TK 대표님**: 위 SQL 패키지 실행 + Vercel env 추가
2. **응답 잘림 영역 재확인**: 운영 데이터 관리 4가지 영역 중 3, 4번 디테일
3. SQL 실행 + 검증 결과 + 누락 디테일 받으면 → 지수가 코드 작업 시작
