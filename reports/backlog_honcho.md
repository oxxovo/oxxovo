# 별건 큐 (backlog) — 지수 본체

신규 생성, 2026-08-10. 이전에 이 이름의 파일은 없었다(본부 확인).
컬럼: 번호 · 요약 · 비용 · 선행조건 · 기한. 상태(open/closed/watch)는 참고용으로 덧붙인다.
회차마다 이 파일을 갱신 — 새 항목 추가·완료 항목은 상태만 closed로 바꾸고 남겨둔다(이력 보존).

## OPEN

### 9/9 전 필수 (묶음 — 상세는 아래 본문 표, 여기는 색인만)

| # | 한 줄 |
|---|---|
| 51 | #48 약속-기능 전수 감사 — 이메일 템플릿 20개 완료, 남음: `/terms`·`/welcome`·`/apply` 나머지 |
| 52 | 제작 과정 기록(edit-event 로그) — 견적 완료, 착수 승인만 대기 |
| 56 | 본선 커뮤니티 투표 — 코드는 정상, 실사용 검증 사실상 0(리허설에서 반드시 태울 것) |
| 59 | 투표 시작 이메일 신설 — 관객·회원용 0개, 배선 설계 완료·제니3 문안 대기 |
| 31 | promo 영상 R2 고아 파일(soft delete #32와 같이 판단) |

| # | 요약 | 비용 | 선행조건 | 기한 |
|---|---|---|---|---|
| 27 | 스트레이 Vercel 프로젝트 `oxxovo-lane-c` — 배포 직후엔 정리하지 않는다(HQ 2026-08-13 명시 지시), 무엇을 남겼는지/왜 지우면 안 되는지 근거 미확인 상태로 존치만 | $0 | 배포 안정화 관찰 기간 이후 | 미정 |
| 28 | **워커 render/generation 레인 — 실제 claim→완료 관측 아직 없음.** 2026-08-13 스위치 ON 직후 확인한 건 "막힌 게 없다"(양쪽 큐 queued/processing/claimed 0건, ffmpeg 핀 일치)뿐이다. 스위치가 막 켜져서 처리할 일감 자체가 없었던 것과 실제로 도는 것은 다른 주장 — 조용한 이유가 "일감 없음"인지 "돌다가 막힘"인지 아직 구분 안 됨 | $0(관측만) | 실참가자 job이 실제로 들어와서 claim→완료까지 가는 순간 | 다음 재개 시 최우선 확인 |
| 24 | `get_active_application_count`(옛 카운트 함수) — `count_active_registrations`로 교체(2026-08-12, `season_registration_reminder` SQL **Run 완료**) 후 호출처 0. DROP 안 함(원 정의를 이 레포에서 못 봤어서 "안전하게 지울 수 있다"를 증명 못 함) — 일정 기간 관찰 후 DROP 후보 | $0 | 없음 | 판단 시점 미정 |
| 25 | 등록수 알림(D-14/7/3/1) 멱등키가 `reminder_day` 값만 본다 — 연기로 `registration_close_at`이 밀려도 이미 보낸 회차(예: D-14)는 새 날짜 기준으로 재발송 안 됨. "역산은 확인했지만 멱등은 마감버전을 안 본다"는 한계, HQ 2026-08-12에 보고했고 이번 범위에서 해결 안 함 | 설계 필요 시 | 실제 연기가 한 번이라도 발동할 때 체감됨(season_0 기준 발동 여지 없음) | 미정 |
| 2 | r2-orphan-sweep에 promo_videos/cf 보호 배선 | 설계 필요, 실행 비용 $0 | **promo·cf 경로가 UUID 기반 파일명을 쓰기 시작할 때** (지금은 파일명에 UUID가 없어 정규식 불일치로 판정 대상 밖 — 조건부, 날짜 아님) | 조건 성립 시 |
| 4 | `getCurrentSeason()` "opened" 아닌 "soonest upcoming" 폴백 경로로만 season_0을 잡는 상태 — 이 기간 과거 open을 가진 어떤 행이든 즉시 가로챔 | $0 (코드 주석 완료, `lib/seasons.ts`) | 감시만, 조치 아님 | **2026-09-09 00:00 PT** 지나면 자연 해소 |
| 5 | award-gate 부분 코호트 확정 — `countBlockingFailed`로 이미 닫혔는지 확인만 (새 작업 아님) | $0 | 없음 | 전체 테스트 착수 전 확인 예정 |
| 6 | email-tick 300초 예산 — 제안 3건(중복 alreadySent 제거 / 틱당 예산+deferred 보고 / 429 처리) | 코드 소량 | 전체 테스트(리허설) 실주행(계산 아닌 실측 필요) | 리허설 전 |
| 7 | 멀티 admin — **마이그는 이미 Run됨**(2026-08-11 실측: `is_staff()` RPC 200·role CHECK 확인). 남은 건 코드 머지뿐(`feat/multi-admin`, `bbc1356`) + 매니저로 올릴 실사용자 부재(현재 profiles 7행 중 실사용자는 대표님 1명, 나머지는 테스트/픽스처 계정) | 머지 1건 | 후보가 될 실사용자가 먼저 가입 | 미정 |
| 23 | 등록 취소(참가자가 스스로 등록을 물릴 수 있는 경로) — HQ 2026-08-12 명시적으로 이번 범위 제외. 정원 카운트+Founding 순번이 등록에 얽혀 있어 취소 시 무엇을 되돌리나(대기자 승격? Founding 번호 반납?)가 별건 설계가 필요 | 설계 필요 | 없음 | 미정 |
| 12 | **전체 테스트(리허설) 착수 시 선행 확인** — season_test·season_test2의 `scoring_start_at`이 스테일이라 버퍼 게이트에 차단된다. 시계 압축 시 그 컬럼도 같이 당겨야 "워커가 조용히 0건"이 안 난다 | 미측정 | 전체 테스트 계획 자체가 아직 없음(2026-08-10 기준, 8/11·8/13·8/14는 본부가 무효화함) | 전체 테스트 착수 시 |
| 16 | 이메일 해지 후 재동의 경로 없음(EmailConsentCard는 해지만, 재동의는 로그아웃 후 재로그인으로 안내) | 설계 필요 시 | 9/9 이후 실사용자가 재동의를 요청하는 사례가 생길 때 | 미정 |
| 17 | 발송 콘솔 완료(`d791091`) / cron 실트리거 미확인 — **#15 게이트는 풀렸다(2026-08-16 실측, 아래 c15b 참조)**. 남은 진짜 이유는 배포가 아니라 데이터: `admin_broadcasts` 실측 0행(2026-08-16) — 아무도 아직 발송 건을 만든 적이 없어 cron이 실제로 처리한 사례 자체가 없다. #28과 같은 모양("막힌 것"과 "일감 없음"을 구분 못함) | $0 | 첫 발송 건이 실제로 만들어지는 순간 | 배포 후 첫 :07/:22/:37/:52 (배포는 됐음, 데이터가 없을 뿐) |
| 22 | `checkApplyGate` 경유로 `lib/studio.ts`가 `next/headers`·JSX(`lib/email/send.tsx`)를 끌어오는 문제, 2026-08-12에 테스트 훅 스텁 2개로 막았다 — **당장은 맞지만 선례가 다르다**: `lib/partners.ts`는 같은 next/headers 문제를 순수 분리(DB-only 헬퍼와 이메일 발신부를 별 파일로)로 풀었다. `lib/membership.ts`도 같은 구조(순수 판정 함수 vs `getUserOrNull` 쿠키 의존)라 분리 여지가 있다 | 리팩터 소량 | 없음, 원할 때 | 미정 |
| 20 | `below_floor`(연기 3회 소진+80 미만) 집행 미정 — 지금은 season-tick 상태전환+예선홀드 자동공개만 막고, 채점/본선/시상은 원래 일정대로 계속 진행됨("성립 안 함"이 실제로 아무것도 안 멈춤). **알림 중복억제는 완료**(`lib/below-floor-alert.ts`, pricing-health의 `alert_state_` 시그니처 패턴, HQ 2026-08-12 지시대로 "알림만, 자동 취소 없음"). 실제 집행(무엇을 할지)은 여전히 대표님 판정 사항, 미정으로 남김(`reports/season_defer_timestamp_audit_2026-08-12.md`) | 판정 필요 | 최초 `below_floor` 발동 전(season_0 기준 3회 연기 소진 시점 — 지금 일정으로는 발동 여지 없음) | 판정 후 |
| 29 | `main_round_twist` 언어별 컬럼 분리 — 단일 컬럼(TEXT 1개, KR/EN 별도 컬럼 없음)이라 사이트 언어 토글이 이 값을 못 바꾼다. 라벨(`main_round_theme_label`)이면 영문 단일 표기로 넘어가지만, 트위스트는 참가자가 읽고 작품을 만들어야 하는 주제라 한쪽 언어만 넣으면 절반이 못 읽는다. 지금은 TK 판정대로 한 컬럼에 EN 먼저 + KR 병기("A scene applying lotion to the face (얼굴에 로션을 바르는 장면)")로 우회(2026-08-14, `reports/season0_main_round_twist_2026-08-14.sql`) | 컬럼 분리 시 스키마+코드 소량 | 없음 | 미정 |
| 31 | `deletePromoVideoAction`이 R2에 저장된 영상은 안 지운다 — Supabase Storage 마커(`/promo-videos/`)가 `video_url`에 있을 때만 Storage 객체를 지우는데, 콘텐츠 영상 91개는 R2 URL(`pub-....r2.dev/promo/...`)이라 이 삭제 액션의 파일-삭제 분기 자체가 안 탄다. 2026-08-14 실삭제 사고에서 발견(`reports/promo_deleted_row_recovery_2026-08-14.sql`) — 이번엔 파일이 안 지워진 게 다행이었지만(행만 복구하면 됨), 구조적으로는 "행은 지웠는데 R2엔 고아 파일이 계속 쌓인다"는 뜻. soft delete(#32 예정) 설계 시 함께 정리 — 완전 삭제(하드 삭제) 단계에서는 R2 객체도 실제로 지우도록 배선 | soft delete 작업에 곁들이면 소량 | 없음, soft delete 설계와 같이 판단 | 9/9 전 |

| 33 | **음악 100곡 파일럿 — Soundverse API 키 403 Invalid로 막힘.** 오늘 안에 키 3개를 순차로 시도(출처불명 워커 키 → 첫 재발급 키 → `platform.soundverse.ai` "Manage keys" 발급 키, PAYG $100 충전 계정) 전부 동일하게 거부됨. 문서상 존재하지 않는 경로(`/v7/status`)와 문서에 있는 실제 생성 엔드포인트(`POST /v5/generate/music/sync`, 문서 예시curl 그대로)를 나란히 대조해도 둘 다 정확히 같은 `403 {"detail":"Invalid API Key"}` — 엔드포인트 문제가 아니라 키 자체가 Soundverse 인증 DB에서 거부되는 상태로 확정, 우리 쪽 원인(Railway 반영·Bearer 헤더 형식) 전부 배제됨. 대표님이 이 근거로 Soundverse에 문의 발송함(2026-08-15). 곡 생성 0건, DB 쓰기 0건, 비용 0. 현재 Railway `oxxovo-studio`(production) `SOUNDVERSE_API_KEY` = platform.soundverse.ai 발급 키(가장 최근 값). **2026-08-16 재검증**: 키 바이트 단위 재확인(공백·개행·따옴표 없음, prefix `sksoundverse_` 일치) + 문서 원문 재대조(`/v5/generate/music/sync`, Bearer 인증 확인) + 신규 curl로 동일 403 재현 — 결론 불변. 키 대기 중 코드 선작업: `oxxovo-studio` `f2b7404`에 (a) Soundverse 어댑터(`src/music-provider-soundverse.ts`, `music-lane.ts` ADAPTERS 등록, 라이선스는 대표님/고문 확정값 대기 상태로 자리만 비워둠, 미배선이라 프로덕션 무영향) (b) 100곡 파일럿 생성 스크립트(`scripts/generate-music-100.ts` — `tracks/batch-100-v1.json` 그리드 그대로 프롬프트 생성, mock 100/100 통과, 실호출 0건) 추가 완료 | $0(시도 실패라 과금 없음) | Soundverse 지원팀 회신 | 회신 오면 실키로 `generate:music-100` 1곡부터, 그 전엔 실호출 안 함 |

| 34 | **레거시 `main_round_video_seconds`(단일 값) 정리.** 2026-07 `season0_clear_video_seconds_2026-07.sql`이 season_0에서 이 컬럼을 NULL로 비우고 DB NOT NULL 제약도 뺐지만, `lib/season-schema.ts:77`이 여전히 `z.coerce.number().int().positive()`(필수)로 검증하고 `SeasonForm.tsx:221`에 편집 칸이 그대로 남아 있어 — 다른 무관한 필드 저장 한 번에 값이 조용히 되살아났다(2026-08-17 실측: season_0 `main_round_video_seconds=30`, `min_seconds=15`가 확정값 30/40과 어긋나 있던 것과 같이 발견). "무해하다"가 아니라 **재발 구조**: ①폼이 이 칸을 필수로 계속 요구하는 한 다음 무관한 저장에서 또 살아난다 ②`app/host/new/actions.ts:195-197`이 시즌 생성 템플릿으로 이 필드를 포함 3개 필드(레거시+min+max) 전부 그대로 복사한다 — 대표님이 시즌1부터 계속 새 시즌/파트너 대회를 여실 계획이라 매번 죽은 값이 새 행에 따라간다. ★min/max는 이미 정상적으로 함께 복사되고 있어 그쪽은 안 고쳐도 됨(2026-08-17 실측, `app/host/new/actions.ts` 196-197행) — 고칠 건 레거시 필드 자체를 스키마·폼·복사 세 군데에서 걷어내는 것. **순서 고정(오늘 뷰 작업과 같은 이유 — 반대로 하면 폼 저장이 검증에서 막힘)**: ①`lib/season-schema.ts` nullable로 변경 + `SeasonForm.tsx`에서 입력칸 제거 + `app/host/new/actions.ts`에서 `main_round_video_seconds: template.main_round_video_seconds,` 복사 줄 삭제 → 배포 ②그 다음에 DB 값을 NULL로 | 코드 소량(스키마 1줄+폼 1줄+host/new 1줄) + SQL 1건(②단계) | ①코드 배포가 ②DB NULL화보다 먼저 | 미정(대표님 판정) |

| 35 | **모델 목록 9/9 동결 (TK 정책).** Seedance 2.5를 지금 등록한 이유가 이것 — 시즌 중 모델을 추가하면 먼저 제출한 사람과 조건이 달라져 공정성·등급 비교가 깨진다. 9/9 이후로는 `model_catalog` 행 추가/제거를 하지 않는다(코드가 막는 게 아니라 운영 규칙). 다음 세션 착수 전 이 날짜를 넘겼는지부터 확인 | $0 | 없음 | **9/9 이후엔 신규 모델 등록 자체를 하지 않는다** |
| 39 | **Watch 그리드 시즌 필터 부재 — 구조적, 재발한다(HQ 2026-08-17 지시).** `getWatchVideos({sort})`가 `seasonId` 없이 전 시즌을 훑는다(`app/watch/ArenaWatch.tsx:41`, `lib/watch.ts:265` `if (opt.seasonId)`뿐 기본 미적용). `watch_hidden`은 행 단위 블랙리스트라 오늘처럼 사후에 하나씩 꺼야 하고, 다음에 "대회 한 바퀴 리허설"용 테스트 시즌을 새로 만드는 순간 그 신규 행들은 처음부터 다시 안 걸린 채로 공개 Watch에 뜬다 — 오늘 51편이 정확히 이 경로로 났다. 리허설(#12, 전체 테스트 착수)이 발사 전 남은 절차 중 하나라 재발 경로가 실존한다 | 코드 소량(seasonId 화이트리스트 또는 `is_fixture`/픽스처 시즌 제외 필터) | 없음, 설계·착수 시점은 대표님 판정 | 미정(대표님 판정) — 역산 안 함 |
| 40 | `model_catalog.sort_order=1`이 `ltx2-fast`(active)와 `veo3.1-fast`(inactive) 둘 다에 걸려 있다. 지금은 inactive라 목록에 안 뜨니 무해하지만, 다음에 `veo3.1-fast`를 켜는 순간 두 모델이 같은 순번을 다퉈 목록 순서가 불안정해진다(2026-08-17 전수조회로 발견) | $0 | 없음 | `veo3.1-fast`를 다시 활성화하기 전 |
| 41 | **참가 규정에 "타인의 저작권 캐릭터·실존 인물 초상 사용 금지"가 명시돼 있지 않다 — 실측 완료(HQ 2026-08-17 지시).** `/rules` §⑥ 실격 목록(워터마크 조작·AI서비스 오표기·비AI영상 위장·중복이메일) 4개 중 어디에도 없음, `official_rules`/`world_championship_rules` 설계 문서 원문에도 0건, `/terms` §5·§6("harmful, illegal, or plagiarized content")도 캐릭터·초상 명시 안 함 — 3곳 전부 확인, 구멍 확정. **채점 쪽도 안 잡는다**: `oxxovo-scoring/src/scorer.ts:239-260` Integrity 기준 원문이 "PROVENANCE IS NOT YOUR JOB"으로 AI 여부 판정 자체를 명시적으로 면제하고, 잡는 건 "제3자 워터마크·방송사 로고·저작권 표시·다른 플랫폼 UI 캡처" 등 **도용 증거**뿐 — 워터마크 없이 AI가 순수 생성한 유명 캐릭터/실존 인물 재현은 위반 증거가 "안 보이므로" integrity=100(만점)이 나가는 구조. 즉 지금은 "AI가 진짜로 그 캐릭터를 만들어낸" 경우를 막을 방법이 규정에도 채점에도 없음 | 규정 문구 추가(코드 소량, `/rules`+`/terms`) + 채점 기준 확장 여부는 별건 판단 | 없음 | 판정 필요(대표님) |
| 42 | **`/watch/rankings` 문안 — 제니3 대기(HQ 2026-08-18 명시).** 지금 세 칸 제목("왜 비어 있는지" / "언제 열리는지" / "그 뒤 어떻게 바뀌는지")은 본부가 이번 작업을 지시하며 쓴 말을 그대로 라벨로 옮긴 것 — 참가자가 보는 화면용 제목이 아니다. 본문도 전부 "제니3 문안 대기" 마커뿐, 지어낸 문장 없음(`app/watch/rankings/page.tsx` `PendingSection`). 제니3 문안 도착 시 **세 칸 제목까지 통째로** 실제 참가자용 문구로 교체 | 코드 소량(문자열 교체) | 제니3 문안 | 문안 도착 시 |
| 43 | `/watch/rankings` 빈 순위표 20줄이 길다 — 10줄 정도로 줄일지 대표님 판단 필요. `platform_config.championship_points_placeholder_rows`(현재 20) 값만 바꾸면 되는 설정값이라 **배포 불필요**, `/admin/settings`에서 숫자만 수정 | $0 | 없음 | 대표님 판단 시 |
| 44 | `platform_config.championship_points_reveal_at = 2027-04-01T00:00:00Z`는 **잠정값**(3월 대회 일정 미확정 상태에서 임시로 넣음, `reports/championship_points_rankings_page_2026-08-18.sql` 주석에도 명시). 3월 대회 일정이 실제로 정해지면 `/admin/settings`에서 이 키를 실제 값으로 갱신해야 함 — 잊혀지지 않도록 항목으로 추적 | $0(어드민 값 갱신만) | 2027년 3월 대회 일정 확정 | 일정 확정 시 |
| 45 | Watch 사이드바 박스의 "2027 Q1" 라벨이 아직 `app/watch/ArenaShell.tsx`의 파일 리터럴(`REVEAL_PERIOD_LABEL`)이다 — `/watch/rankings`는 이미 `championship_points_reveal_at` 설정값을 읽어 표시하는데(#44), 사이드바 쪽은 그 작업 범위 밖이라 안 건드렸다. 같은 설정값을 사이드바(클라이언트 컴포넌트)까지 끌어오려면 `ArenaWatch.tsx`(서버)가 그 값을 읽어 prop으로 내려주는 배선이 필요 — 대표님이 물어본 "설정값으로 뺄 수 있나"의 답은 "가능하다, 다음 손댈 때"이고 이번엔 범위 밖이라 미착수 | 코드 소량(prop 배선 1단) | 없음 | 대표님 판단 시 |
| 46 | **규율 정정 — "SQL은 ASCII만"은 코드·주석에 대한 규율이지, 저장할 값 자체에 대한 규율이 아니다.** 2026-08-19 `faq_banned_words_warning` 최초 SQL을 `탈락` 같은 유니코드 이스케이프로 넣었다가 그대로 리터럴 문자열로 저장됨(실측: `findBannedWords`가 "탈락"을 0건 매치) — Postgres는 plain `text` 컬럼에서 `\uXXXX`를 안 풀어준다(JSON/JSONB 값이거나 클라이언트 `JSON.parse`를 거칠 때만 풀림). 대표님이 한글 리터럴 그대로 재실행 후 재검증(`scripts/verify-faq-banned-words.mjs`, 9/9 통과 — 탈락/아쉽게/아쉽게도/결선/Kling/up to $250,000 전부 정상 매치, 클린 텍스트 false positive 0건)으로 해소. **앞으로**: SQL 파일의 코드·주석은 ASCII만, 저장 값이 한글이면 그 값은 한글 그대로 넣는다 | $0(해소됨, 기록용) | 없음 | 완료, 재발 방지 기록 |
| 47 | **`faq-banned-words.ts` 적용 범위 경계 — 랜딩(`/`) FAQ 9문항 CMS 저장(`saveFaqItemAction`)에만 걸린다.** 캡션·이메일 문구·포스터 등 제니3가 직접 쓰는 다른 카피에는 이 가드가 물리적으로 걸리지 않는다(코드로 검사하는 곳이 이 한 자리뿐) — 그런 카피의 금지어 회피는 문안 작성 시점의 제니3 수동 규율이지 시스템 가드가 아니다. 정적 `/faq`(15문항, `app/faq/page.tsx`)도 이 가드 밖(하드코딩, DB 미경유) | $0(경계 기록) | 없음 | 새 카피 채널(이메일/포스터 등)에 같은 가드가 필요해지면 재판단 |
| 51 | **#48 약속-기능 전수 감사 — 이메일 템플릿 20개 완료(2026-08-20, 4개조 병렬 감사).** `/faq`·랜딩FAQ·멤버십 페이지도 완료. 남음: `/terms`·`/welcome`·`/apply` 나머지 상태·가입-결제 흐름 전체·Studio 인앱 문구·어드민 수동발송 문안. 방법=문장단위 추출 후 기능대조(grep 금지) | 미측정 | 없음 | **9/9 전 필수** |
| 52 | **본부 9/9 전 지시 — "제작 과정을 남기고 있나" 실태 조사 완료, 결손 1건 확정(2026-08-19).** 6항목 실측(`generation_jobs`/`render_jobs` 스키마 전수 확인): ①생성기록 있음(`reports/studio_phase1_migration_2026-06.sql:75-123`) ②버린 클립도 있음 — `deleted_at`/`archived_at`이 soft delete라 로우·R2 파일 삭제 없음(`studio_deleted_at_2026-07.sql`/`studio_archived_at_2026-07.sql`), `render_jobs.source_job_ids`와 대조하면 미채택 클립도 재구성 가능(직접 "버림" 플래그는 없음, join으로 도출) ⑤수정횟수 — 저장 카운터는 없지만 로우가 안 지워지므로 언제든 `COUNT(generation_jobs)`로 파생 가능(`lib/studio.ts:398` `countGenerationsForRound`), 결손 아님. **★진짜 결손은 ③④ 하나로 수렴**: `render_jobs.edl`은 렌더 버튼을 누른 순간의 스냅샷 1개만 저장(`reports/studio_compose_phase1_migration_2026-06.sql:26-69`)하고 그 컬럼은 이후 업데이트도 안 됨 — 렌더 누르기 전 에디터에서의 순서 재배치·트림 조정은 DB에 전혀 안 남고 브라우저 localStorage 드래프트에만 존재(`app/studio/actions.ts:703-708`), 세션 종료·기기 변경 시 그 편집 과정 자체가 증발한다. 렌더를 여러 번 누르면 매번 새 행이 생겨(update 아닌 insert, `lib/studio.ts:1881`) 시도별 우연한 이력은 남지만, 한 번만 렌더하고 끝내면 "몇 번 재배열했는지/트림을 몇 번 조정했는지"는 영구 소실. ⑥세션 시간도 전용 로그 없음 — `created_at`/`worker_started_at` 등 점 단위 타임스탬프로만 근사 가능(activity/session 테이블 grep 0건, 128개 마이그 전수). **견적**: 신규 테이블 필요(append-only 편집 이벤트 로그 — reorder/trim-commit/clip add·remove를 지금 저장 안 되는 이벤트로 기록. 기존 두 테이블과 grain이 달라 컬럼 추가로는 안 풀림), 용량은 이벤트 1행 수백 바이트 × 참가자당 라운드당 수십~백여 건으로 잡아도 전체 수만~십만 행(수십MB) 수준이라 무시 가능. 점수 반영 없음(본부 명시), 기록 전용. 작업 규모 자체는 작음(마이그 1개 + insert 서버 액션 1개 + 에디터 핸들러 3~4곳 배선)이나 정확한 소요는 착수 전 단정 안 함 | 설계+코드 소량 (신규 테이블 1개) | 대표님 착수 승인 | **9/9 전(본부 지정)** |

| 53 | **다운로드+워터마크 기능 — 설계 승인 완료, 코드 절반만 진행, 2개 블로커로 멈춤.** 설계(①참가자 다운로드=로고만·②홍보용=로고+닉네임, 게이트=`awards_announcement_at`, 온디맨드+캐시, `render_jobs` 5컬럼+`download_requested_at`/`promo_requested_at` 마이그 완료·Run 완료) 전부 TK 승인. 워커 쪽 ffmpeg 오버레이 함수(`applyDownloadWatermark`/`applyPromoWatermark`, Pretendard "by {닉네임}" 래스터화 포함)는 `oxxovo-studio`에 작성·커밋·push 완료(`e6c3fab`, 2026-08-19) — 단 worker.ts 어디서도 안 부르는 미사용 함수라 이 push는 워커 동작에 영향 없음(Railway push=배포 규칙상 배포는 됐지만 no-op). ★블로커①: 로고 애셋 크롭 여부 미확정(원본 `oxxovo_logo.png` 그대로 쓰는지, promo 포스터 작업이 쓴 크롭이 정본인지 제니3 미답). ★블로커②: 워커 폴링 lane(`watermarkLane`, `claimNextWatermarkJob` 등) 자체를 아직 안 짬 — render.ts 함수만 있고 이걸 부르는 자리가 없음. `/profile` 다운로드 버튼도 미착수 | 코드 중간 규모(워커 lane+website 버튼) | 로고 크롭 확정(제니3) | 미정 |
| 54 | **어드민 강제 닉네임 수정 — 설계만, 코드 없음.** 출품 시 `display_name_locked_at`로 잠기는데, 잠긴 뒤 오타 등을 고칠 관리자 액션이 없음(2026-08-19 확인, "다음 라운드로"로 합의 — 잠기기 전엔 온보딩 게이트가 막아주므로 급한 구멍은 아님) | 코드 소량(어드민 액션 1개) | 없음 | 다음 라운드 |
| 55 | **닉네임 전용 신고 경로 — 없음.** `VideoReportButton`(`app/watch/SaveReportButtons.tsx`)이 영상 신고는 되지만 사유 분류·닉네임 전용 경로가 없음 — 금지어 목록이 완전할 수 없어 새어나간 이름을 참가자/관객이 신고할 방법이 현재 0개(2026-08-19 확인) | 설계 필요 | 없음 | 미정 |
| 58 | **본선 채점 "완료" 컬럼이 없다 — 시상 전에 안 끝나도 아무도 모른다.** 예선은 `scoring_start_at`/`scoring_complete_at` 짝이 있는데, 본선은 시작 게이트(`community_vote_start_at`, `oxxovo-scoring/src/gate.ts:33-41,60-61`)만 있고 짝이 되는 완료 컬럼이 없음 — `awards_announcement_at`(11/16 20:00 PT) 전에 본선 채점이 실제로 끝났는지 확인할 방법이 코드에 없다(2026-08-20 일정 감사에서 발견) | 설계 필요(신규 컬럼 + 게이트 판단) | 없음 | 미정, 급하지 않음 |
| 56 | **본선 커뮤니티 투표 — 코드는 정상(라이브 가중치·날짜게이트·집계·UI 전부 배선 확인, 2026-08-20 실측), 실사용 검증은 사실상 0.** `watch_votes` 총 1행(round=main) — 배포 이후 실참가자 경로로 끝까지 도는 것을 본 적이 없다. `lib/scoring.ts`의 computeFinalScore는 weight>0이면 커뮤니티 집계가 실존해야 final_score가 나오고, 없으면 null(랭킹 제외) — 아무도 안 투표하면 본선 랭킹 전체가 빈다(코드 주석에 이미 경고됨, 2026-08-06). **전체 리허설(#12)에서 반드시 투표 캐스팅→집계→final_score 산출까지 실사용 경로로 태워야 한다** — HQ 2026-08-20 지시 | $0(리허설 항목 추가만) | 리허설 착수(#12) | **리허설 시 필수, 9/9 전** |
| 57 | **`ResultsAnnounced.tsx` "프로필에서 최종 순위 확인 가능"이 과장 — `/profile`엔 순위 UI가 없다.** `ScoringCard.tsx`는 점수·세부항목만 노출, top-3 `award_rank` 배지 외에는 "500명 중 N위" 같은 순위 표시가 없음(2026-08-20 #48 이메일 템플릿 감사에서 발견). `NotSelected.tsx`는 발송 시점에 순위를 직접 계산해 이메일 본문에 박아 넣는 걸 보면 데이터 자체는 있음 — 프로필에 노출만 안 함. 문안을 낮출지(순위 언급 삭제) `/profile`에 순위 UI를 새로 만들지는 대표님 판정 | 문안이면 $0, 기능이면 코드 소량(프로필 순위 UI) | 대표님 판정 | 판정 필요 |
| 59 | **투표 시작 이메일 신설 — 관객·회원용 없음.** 참가자용은 `VideoLiveMain.tsx`가 이미 커버(단 #60 타이밍 결함 있음). 관객·회원용은 0개 — 투표가 점수의 절반인데 투표하라는 안내가 나가는 곳이 없음. 배선 자리 설계 완료(`app/api/cron/email-tick/route.ts`에 `fireResultsAnnounced`와 같은 모양으로 `community_vote_start_at` 게이트 신설, 수신자=`profiles.email_opt_in=true AND email_opt_out_at IS NULL`, 기존 발송 콘솔 동의 기준 재사용) | 코드 소량(배선) + 신규 이메일 템플릿 | 제니3 문안 | **9/9 전 필수** |
| 63 | **결제 실패(past_due 진입) 시 능동 알림 이메일이 없다 — HQ 2026-08-22 확정, 착수 승인.** `app/api/membership/stripe-webhook/route.ts`가 `invoice.payment_failed`를 받아 `membership_status='past_due'`로 쓰기만 하고(`lib/membership-billing.ts:285-296`) 이메일은 안 보낸다 — 화면 차단(`membership_required`, CTA `/apply`로 확인됨)은 있지만 참가자가 Studio를 직접 열어봐야만 안다. **HQ 판정: 결제 끊긴 순간 ①메일 ②화면 둘 다 알린다** — 화면은 이미 됨, 메일은 이 웹훅에서 발송(⑤ 이메일 작업에 묶어 처리) | 코드 소량(웹훅에 이메일 1건 추가) + 신규 템플릿 | 제니3 문안 | **9/9 전 필수** |
| 64 | **Watch 필수조건(Twist) 배너 — 함수 레벨 실측만 완료, 브라우저 렌더는 미확인.** 2026-08-22 `getRevealedTheme()`을 season_test에 임시 값(공개 전/후 두 상태) 넣어 직접 왕복 검증(공개 전 `twistRevealed:false,twist:null` / 공개 후 `true,"테스트문자열"` 확인 후 원상복구), 서버가 값을 내려주는지는 확실함. **브라우저 실제 렌더는 확인 못 함** — DB 전체에 `watch_hidden=false`인 본선 영상이 0건이라(#39와 같은 기존 원인, 오늘 건드린 것과 무관) `/watch/[id]` 자체에 못 들어감. `watch_hidden`은 승인 없이 안 건드림. **HQ 지시: 전체 리허설(#12) 착수 시 반드시 브라우저로 태울 것** — 실제 본선 영상이 watch_hidden=false로 존재하게 되는 시점(리허설 또는 실참가자)에 확인 | $0(리허설 항목 추가만) | 리허설 착수(#12) 또는 실제 본선 영상 존재 | **리허설 시 필수** |
| 65 | **실격 게이트 스키마 — HQ 2026-08-28 확인: 두 제약은 별건, 섞지 않는다. `information_schema` 프로브 완료(2026-08-28), 5개 전부 MISSING.** (a) `main_round_disqualify_missing_votes`에 `CHECK` 1~3 — 이게 HQ가 말한 것. `seasons.main_round_required_elements`/`main_round_disqualify_missing_votes`/`main_round_disqualify_appeal_hours`/`main_round_disqualify_enabled`, 테이블 `main_round_disqualification_events` **5개 전부 라이브 DB에 없음**(`scripts/zz_probe_disqualify_gate_schema_2026-08-28.mjs`, PostgREST 42703/PGRST205로 실측) — 8/23 SQL은 실제로 안 돌았다(HQ 추정 확인됨). ★8/23 SQL 원문 자체를 레포·메모리 어디서도 못 찾음(2026-08-24 EOD `⑥`엔 "CHECK 1~3 추가" 제목만 남아있고 본문 없음) — 타입·기본값·이벤트테이블 컬럼 구성은 DRAFT로 재구성해 2026-08-28 채팅으로 올림, **HQ 확인 전엔 Run 금지**. (b) 지수가 별도로 제안한 `disqualification_reason` 3-값 제약 — 별건. 상세가 이 리포지토리에도 메모리에도 안 남아 있음 — 다음 착수 전에 원안부터 복원 필요 | 미정(HQ가 DRAFT 확인 또는 원안 제공 후) | (a) HQ가 DRAFT 확인, (b) 원안 복원 | 실격 게이트 착수 시 |
| 66 | **참가작(`genesis_applications`) 시드는 반드시 추적 가능한 스크립트로 — HQ 2026-08-28 지시.** season_test 41편(2026-06-04 유입, `/promo/content_video/` 92편 풀 재사용)이 어떤 커밋된 스크립트에서도 나온 흔적이 없어 유입 경로 자체가 추적 불가능했다(#39/■1 leak의 근본 원인). **앞으로 원칙: 직접 SQL INSERT/UPDATE로 이 테이블에 행을 넣지 않는다 — 항상 커밋된 스크립트(`scripts/` 또는 루트 `_*.mjs`) 경유**, git log에 누가/언제/무엇을 넣었는지 남도록. 기존에 이미 있는 패턴(`_main_round_seed.mjs`/`_season_test2_setup.mjs`/`_watch_demo_insert.mjs`/`_watch_gallery_insert.mjs`)을 그대로 계속 쓰면 됨, 새 인프라 필요 없음 | $0(운영 규율) | 없음 | 즉시 적용, 다음 시드부터 |
| 67 | **정정(2026-08-30, 본부 지적): #67 원판의 "벤더 revoke" 결론은 틀렸다 — 내가 잘못된 키를 쳤다.** 대시보드 실측(대표님): 키 라벨 `oxxovo-worker`(끝 6자 `-pPlqlx`)가 USAGE 27·Aug 29 생성으로 살아 있고, 어제 실호출 27건이 전부 그 키로 기록돼 있음. 그런데 오늘 내가 `GET /v1/files/{file_id}/url`·대조군 `/v1/account/balance`에 쓴 `SOUNDVERSE_API_KEY`(Railway `trustworthy-enchantment/oxxovo-studio`, production env)는 끝 6자가 `N_FnlG`로 **완전히 다른 키**였다(401은 이 값 기준으로는 맞는 반응 — revoke가 아니라 애초에 다른/틀린 키). Railway 프로젝트 4개(`charming-recreation`/`fulfilling-consideration`/`just-vibrancy`/`trustworthy-enchantment`) 전부의 `oxxovo-studio` 서비스를 대조해봤는데 `SOUNDVERSE_API_KEY`가 설정된 곳은 `trustworthy-enchantment` 하나뿐이고 그 값도 `-pPlqlx`가 아니다 — **즉 어제 27건을 실제로 만든 `-pPlqlx` 키는 이 4개 Railway 프로젝트 중 어디에도 영속 저장돼 있지 않다.** 어제 세션이 "임시 키 파일을 삭제했다"고 한 것과 정황이 맞음 — 로컬 임시 파일로만 넘겨 썼고 Railway엔 반영 안 됐을 가능성. **결론: 벤더 이슈 아님, 우리 쪽 키 관리 공백.** 다음 조치: 대표님이 대시보드에서 `-pPlqlx` 키 실값을 확보해 `railway variables --set`(트러스트워디-인챈트먼트 프로젝트, `oxxovo-studio`, production)으로 영속 반영 → 그 다음에야 `/url` 재확인 가능 | $0(확인만, 다음은 대표님 키 반영) | 대표님이 `-pPlqlx` 실값을 Railway에 세팅 | 다음 Soundverse 작업 재개 시 |
| 68 | **`flag_spread_threshold` = 죽은 시즌 컬럼 — HQ 2026-08-30 지시, "읽는 곳이 없는 시즌 컬럼" 전수 감사로 확대.** `seasons.flag_spread_threshold`(어드민 폼 `SeasonForm.tsx`에 "Spread flag threshold"/"편차 플래그 임계값" 입력 필드로 노출)를 `oxxovo-scoring` 워커 전체(`grep`)에서 0건 참조 — 어드민에서 값을 바꿔도 채점 파이프라인엔 아무 영향이 없다(`scoreWithAllAIs`가 3사 산술평균만 하고 이상치 배제 로직 자체가 없음, 오늘 실측). **오늘 아침 커밋(`a235ce9`)으로 고친 `main_round_video_seconds`(admin 폼에서 제거된 dead gate)와 같은 병** — "어드민 필드는 있는데 아무도 안 읽는다"가 반복 패턴으로 보임. **요청: `seasons` 테이블 전체 컬럼을 어드민 폼 노출 여부 × 실제 읽는 코드(앱+워커) 존재 여부로 대조하는 전수 감사** — `flag_integrity_threshold`처럼 실제로 읽히는 짝(`scorer.ts`의 `reviewThreshold`)과 나란히 있어서 안 읽히는 컬럼도 읽히는 것처럼 보이는 함정이 있음, 이번처럼 개별 발견이 아니라 목록으로 한 번에 뽑아야 재발을 막음 | 감사 자체는 코드 변경 없음(조사), 발견되는 죽은 컬럼별로 후속 정리 필요 | 없음 | 다음 착수 시 |

## CLOSED (기록 보존)

| # | 요약 | 종결 근거 |
|---|---|---|
| c-votegap | 61. 투표 종료 → 시상 발표 20시간 — 늘릴지 판정 필요했던 항목 | **2026-08-22 ① 일정 전면 갱신으로 해소.** `community_vote_end_at`이 11/16 17:00 PT, `awards_announcement_at`이 11/18 17:00 PT로 옮겨져 간격이 20시간 -> 48시간(2일)이 됐다(`reports/season0_schedule_finalize_2026-08-22.sql`). 판정 불필요 |
| c-membershipgrace | 62. 멤버십이 제작 도중(마감 임박) 끊기는 경우 — 유예 신설 여부 | **HQ 2026-08-22 최종 판정: 유예 없음, 확정.** 결제가 끊기면(`isActiveCreator`=false) Studio는 즉시 막힌다 — `checkStudioAccess`가 이미 그렇게 동작한다(추가 코드 없음). 직전 회차가 이걸 "시즌1 전 재검토"로 열어둔 게 잘못이었다는 HQ 지적 — 재검토할 정책 자체가 없다, 종결. 별건으로 갈라짐: 결제 실패 시점의 **능동 알림 이메일**(Stripe `invoice.payment_failed` 웹훅에 이메일 발송 없음, 실측 확인)은 존재하지 않음 -> #63로 분리 |
| c-themeleak2 | `seasons_public`이 `main_round_theme_label`("Cosmetic Commercial Film")을 anon key로 그대로 유출 — 08-17 뷰 정리(main_round_theme 제거) 이후에도 라벨 자체는 "안 새는 것"으로 판정돼 남아 있었는데, 실제로는 로그인 없이 REST로 바로 읽혔음(실측 재현) | **2026-08-21 처리.** BLOCK1: `DROP VIEW`+`CREATE VIEW`(라벨 컬럼 제외)+`GRANT` 한 덩이로 Run 완료(`reports/seasons_public_theme_label_removal_2026-08-21.sql`). anon key curl 재검증 2회(수정 직후·최종) — 응답에 컬럼 자체가 없고 "cosmetic" 문자열 0건. `/`·`/watch`·`/welcome` 정상 렌더 확인(200, 실콘텐츠). 앱 코드 전수 grep으로 `seasons_public`에서 이 컬럼을 이름으로 select하는 곳 0곳 확인 후 진행(회귀 없음) |
| c-seasonsrevoke | `authenticated`가 `seasons`에 INSERT/UPDATE/DELETE grant를 갖고 SELECT는 없는 상태 — 거꾸로였음. 로그인 유저가 시즌 행을 실제로 고칠 수 있는지 미확인 상태 | **2026-08-21 처리.** 실계정(디스포저블)으로 실측: SELECT/UPDATE/DELETE는 이미 안 됨(SELECT 없어서 WHERE 평가 불가), INSERT만 grant는 있었지만 RLS가 막고 있었음(당시는 안전, 구조는 위험). `REVOKE INSERT, UPDATE, DELETE ON public.seasons FROM authenticated` Run 완료(`reports/seasons_authenticated_write_revoke_2026-08-21.sql`, 되돌리는 GRANT는 같은 파일 하단에 별도 블록으로만 존재, 실행 안 됨). Run 후 재검증: 같은 계정으로 INSERT 재시도 시 에러가 RLS위반(42501 row-level security)에서 grant부재(42501 permission denied, hint="GRANT INSERT...")로 바뀐 것을 확인 — REVOKE 반영 실측 확인 |
| c-themeleakci | `theme-leak-check.mjs` Part2(라이브 seasons_public API 스캔, 전날 추가)가 CI에서 한 번도 실제로 돈 적 없음 — env 없이 실행하면 조용히 `WARN...skipped`, CI의 `npm test`는 이 스크립트 자체를 호출한 적이 없어서 코드는 맞게 짜여 있었는데 아무도(사람도 CI도) 실제 env로 돌린 적이 없었던 것 | **2026-08-21 처리.** `.github/workflows/checks.yml`에 전용 스텝 추가(`773cde6`) — 실제 anon key/URL(NEXT_PUBLIC_*라 시크릿 아님) 그대로 사용, 매 push마다 라이브 `seasons_public`을 실제로 때림. `npm run test:theme-leak`를 `--env-file` 없이 돌리면 Part2가 스킵된다는 것도 이번에 확인(재발 방지 근거) |
| c-logo3 | #53(다운로드/홍보 워터마크) 블로커① 로고 애셋 — 크롭 여부 미확정 상태로 대기 중 | **2026-08-21 산출물 완료.** `logo-wm-white.png`/`logo-wm-black.png`/`logo-wm-white-thick.png`(투명 배경, 2400×1693 RGBA, Patent Pending 배지 제거, 8% 여백 크롭) — `C:\Users\Tom\oxxovo\outputs\`. ★원본 `logo-black.png`이 실제로는 알파 255(완전 불투명)+체커보드가 픽셀에 구워진 파일이었음(진짜 투명 아님) — 그레이스케일 분포로 알파 재구성. thick 변형은 처음에 전체 dilate했다가 고리 안쪽 음각 디테일이 점박이로 뭉개져서 폐기, 회로 가지/노드 영역에만 공간 제한해서 재작업. 우하단·4%여백·70%불투명도·8/10/12% 검증 샘플도 실제 Watch 프레임(season_test 포스터) 위에 제작. **★크기(8/10/12%) 확정과 TM 표기 여부는 대표님 판정 대기 — 이 항목은 "산출물 완료"로 닫되, 판정 후 실제 파일 채택은 별건** |
| c-videolivetiming | "VideoLiveMain 타이밍이 community_vote_start_at을 안 본다"(#60) — **정정: 틀린 항목이었다.** | `lib/video-live.ts:36-51` `videoLiveRounds()`가 main 라운드를 `isVotingOpen()`(community_vote_start_at/_end_at 직접 검사)으로 이미 게이트 — 주석에 이유까지 이미 적혀 있었음. #48 배치4 감사와 그걸 이어받은 판단 둘 다 `fireVideoLive` 상단 주석(prelim 공개판정 얘기)만 읽고 `videoLiveRounds()` 내부 분기는 안 열어본 채 단정한 오류. 코드 변경 없음, 배포 없음 |
| c49-50 | 49/50. 3레포 배포 재검증 + 채점 레포 "33커밋" | **2026-08-20 실측으로 완결.** 전부 새로 fetch + Railway CLI: 프로덕션이 이미 origin/main과 일치(`f3eec3d`/`e6c3fab`), "33커밋 미반영"은 로컬-vs-origin 오독이었고 실제 미반영은 `feat/main-round-worker` 2커밋(`6c293da` 배점 프롬프트 동기화)뿐 — merge-tree 드라이런 충돌 0, 병합·push·배포(`47c3941`) 완료 |
| c-scoreweight | 배점 배포 — DB 30/45/25/0인데 프롬프트가 25/45/20/10 하드코딩이던 어긋남 | `6c293da` 병합·배포. `buildScoringPrompt()`를 실제 season 행으로 직접 호출하는 읽기전용 프로브(`_verify_prompt_weights_2026-08-20.ts`, 커밋 `472e519`)로 프롬프트 문자열 자체가 30/45/25를 찍는 것까지 실측 확인(LLM 호출 0회) |
| c-configcache | 설정값 캐시 60초 TTL — ip-check·cosmetic guard 핫패스만 | `getPlatformConfigMap()`(`lib/partners.ts`)에 60초 TTL, 에러는 캐시 안 함. `/admin/settings`에 해당 6키만 "캐시" 뱃지+안내. tsc clean·564/564, 배포(`0979d0a`) |
| c-scorelabel | `/rules`·`lib/admin-i18n.ts`에 남은 25/45/20/10·Integrity 10% 표기 | `/rules`는 "Integrity도 4번째 가중치"라는 프레이밍만 수정(숫자 자체는 원래도 동적), `admin-i18n.ts` 4개 라벨은 퍼센트 통째로 제거(숫자 정정이 아니라 재발 구조 자체를 없앰). 배포(`0979d0a`) |
| c-waitlistcopy | `Waitlisted.tsx` 대기자 자동승격 허위 약속 — fork가 임의 문구로 1차 수정한 것을 제니3 확정본으로 재교체 | 제니3 확정 문안(KO/EN, "자리가 중간에 열리지는 않습니다" 포함)으로 배포(`e9b8bc6`) |
| c-defergate | 연기 판정이 `application_close_at`(제출마감)에 발동 — 참가자가 영상 다 만든 뒤 통보되는 구조 | 코드(`app/api/cron/season-tick/route.ts`, `8c7d478`) 배포 완료: 게이트를 `registration_close_at`으로 이전. **DB SQL은 대표님 Run 대기**(`reports/season_defer_gate_registration_close_2026-08-20.sql`) — Run 전까지는 무해한 대기 상태(RPC 자체 게이트 불변) |
| c15b | 15. 앱 배포(+283 커밋 누적분, `37c7f8f` 포함) | **2026-08-16 실측.** `curl www.oxxovo.ai/api/version` → `{"sha":"bb5e748","builtAt":"2026-08-16T03:03:31Z"}`. `git merge-base --is-ancestor 37c7f8f bb5e748` = YES — [C]④ 게이트 통일 커밋이 현재 라이브 SHA의 조상 확인, 추측 아니라 git ancestry로 증명. `vercel.json`의 `deploymentEnabled.main=false`(git push 자동배포 없음, 수동 `vercel --prod`만) 재확인, 이 배포는 그 수동 절차로 이뤄짐 |
| c14b | 14. [C]④ Pro Editor(H 속도램프 6단계+조합버그+오디오 리타이밍+배포+폰트+게이트통일 전부) | **앱 측 = c15b와 동일 증거(37c7f8f 라이브 확인).** 워커 측 = `oxxovo-studio` H 전체 포함 머지 커밋(`71dabc7`)이 이번 창 시작 시점 `origin/main`에 이미 존재(내가 새 커밋 올리기 전에 fetch로 확인) + Railway는 push=배포(레포별 확립된 규칙, `railway_deploy_guide_oxxovo_studio.md`) — 이번 창에서 그 위에 커밋을 얹어 push했을 때 Railway가 즉시 빌드 트리거하는 것도 직접 관찰(`railway status`로 Building 확인), 파이프라인이 살아있고 그 커밋이 배포 경로를 이미 탔다는 뜻 |
| c3b | 3. `/about` 정적 프리렌더 스테일 | **2026-08-16 실측으로 해소 확인.** c15b/c14와 같은 라이브 SHA(`bb5e748`, 8/13 이후 다회 배포 확인) — "다음 프로덕션 배포 시 자동 해소" 조건이 이미 여러 번 충족됨. 재발 조건은 원문 그대로 유효(다음에 정적 프리렌더 페이지가 시즌 전환 직전 배포 없이 오래 버티면 같은 증상 재발 가능 — 구조적 결함 자체는 안 고쳐짐, 이번 폐쇄는 "지금 스테일하지 않다"는 관측일 뿐) |
| c30 | 30. Stripe 결제가 "아직 test mode"라던 이 항목 자체가 틀렸다 | **2026-08-15 정정.** 이 항목을 쓴 2026-08-14 당시엔 코드(`isTestMode()`)만 보고 실제 Vercel env 값은 확인 안 한 채 "test mode"라고 단정했다(★근거의 범위를 확인 안 한 사례). 2026-08-15 실측: Production/Preview `STRIPE_SECRET_KEY` 전부 `sk_live_` 접두어(대표님 Stripe 대시보드 `pk_live_...2ozd`, 계정도 live). 사이트도 계정도 이미 live였다 — 대표님이 그 사이 직접 전환하신 것으로 보이고 이 문서가 못 따라갔다. 실제로 남아있던 문제는 다른 것: `app/studio/page.tsx`의 "test mode" 라벨이 하드코딩이라 실제 키 모드와 무관하게 항상 떠 있었다(7/13 기록된 결함, `reports/studio_stripe_launch_blocker_2026-07.md` fix (a) 미적용 상태로 방치돼 있었음). `studio_purchase_enabled=true`인데 라벨은 test mode라고 거짓말하는 상태로 있었던 것 — 다행히 실결제 0건(도달 가능한 사람이 대표님뿐이라 노출 자체가 없었음). 라벨을 실제 `isTestMode()`로 배선(`app/studio/actions.ts` `getPurchaseOptions()`가 `testMode` 내려줌, `BuyCredits`가 그 값일 때만 태그 렌더) + 로컬 대조군(`lib/stripe.test.ts`, 키를 test/live 양방향으로 바꿔 라벨 조건이 실제로 뒤집히는지 확인, 프로덕션 env 미접촉)으로 고쳐서 배포함. `studio_purchase_enabled`는 안 내림(판정: 공개 게이트가 닫혀 있어 대표님 외 도달자 없음, 내리면 대표님이 결제 확인을 못 함) |
| c10b | `lib/email/schedule-lines.ts` "결과 안내" bullet 부재 | `6256e33`(2026-08-08 최초 배선, 결과안내 컬럼 없어 미포함)→`d918402`(같은 날, AI심사 컬럼 타입 누락 수정)→`3610be3`(2026-08-10, `prelim_results_announcement_at` 전용 컬럼 확보로 결과안내 불릿 추가). 2026-08-13 이 창에서 season_0 실값(`prelim_results_announcement_at=2026-11-08T20:00:00Z`)을 `formatScheduleMoment`/`prelimReceiptLines`에 직접 통과시켜 재검증: KR "11월 9일 오전 5시(한국 시간)" / EN "Nov 8, 12:00 PM PT" — 세 불릿(공개/AI심사/결과안내) 전부 각자 컬럼에서 렌더, 타이핑된 리터럴 없음. DB 쓰기 0 |
| c21 | 21. 멤버십 게이트를 실제 참가 경로(`registerForSeason`)에 배선 | HQ 2026-08-12 지시대로 집행(판정 아님, 이미 확정된 정책): `checkApplyGate`를 `registerForSeason`/`submitGeneration` 5a/`submitRender` 7a 세 곳(모두 신규 행 mint 시점, 정원 판정과 같은 자리) 전부에 배선, fail-closed(`getMembershipState`가 이미 그렇게 설계돼 있음, 수정 불필요). 실측 검증: `scripts/zz_probe_membership_gate_2026-08-12.mjs` — Founding 캡을 `claimed+1`로 실측 낮춰 실제 101번째(zz_ B)가 `registerForSeason`에서 `membership_required`로 막히고 100번째(zz_ A, 실제 Founding 클레임)는 통과하는 것을 확인 후 전부 원복(카운터 2→1, 캡 100 복귀, zz_ 계정/행 0건 잔존 확인). 부수: 이 배선이 `lib/dst-boundaries.test.ts`를 깨서(`lib/studio.ts`가 새로 끌어온 `next/headers`+`lib/email/send.tsx`를 `scripts/test-hooks.mjs`가 스텁 안 하고 있었음) 그 두 스텁을 공유 테스트 훅에 추가, 507/507 복구 |
| c1 | `in_progress` 영구 skip (lease/타임아웃 부재) | PR #3 lease 4단계, 2026-08-09 프로덕션 로그로 확인(`ITEM_DEADLINE=1389s LEASE_STALE=46min`) |
| c2 | 채점 재현성 3건(Gemini 아암 D 채택 / 모델ID 고정 / Gemini 대응) | 본부 판정 완료. 2026-08-10 Defect1(b) 대조군 실측이 사후 실증 |
| c3 | 렌더 워커(oxxovo-studio) 12커밋 배포 | 배포할 것 없음 — main tip 2069b8df 이미 ACTIVE (2026-08-09 TK 확인) |
| c4 | 예선 41행 전체 재채점 | 불필요 판정(season_0 scoring_results 0행, 41행은 리허설 데이터). 인용 시 "temp 0 채택(8/6) 이전 설정" 축 표기 규율만 남음 — [[feedback-scoring-evidence-settings-era]] |
| c5 | Execution criterion 4 "External uploads cannot be entered at all" 하드코딩 | 지금 안 고침. 종료조건 주석 추가 완료(`scorer.ts`) — 파트너 시즌이 외부 플랫폼을 허용하는 순간 재작업 |
| c6 | community_vote_start_at/end_at 날짜 정정 | Run 완료. BLOCK 0에서 이미 11/13·11/16로 바뀌어 있던 것 확인(레포 밖 집행) |
| c7 | season_0.application_open_at 9/9 여부 판단 | 옮기지 않기로 확정(레포 밖에서 이미 9/9로 집행돼 있었음) — 항목 4로 위험만 추적 |
| c8 | season_test·season_test2 application_open_at NULL 처방 | Run 완료. `getCurrentSeason()` SQL 재현 검증으로 `current_season_id='season_0'` 확인 |
| c9 | official_actors 옛 R2 오브젝트 4개(consistency_i2v.mp4 제외) 삭제 | 실행 완료. 신구 경로 HEAD 재확인(옛 4개=404, 신규=200, mp4=200 무변화) |
| c10 | 11월 운영비 산출 | **폐기(TK 판정, 2026-08-10)** — "필요없는 것에 시간·돈 낭비 말라". 근거: 회사 부담 항목은 이미 실측 존재(인프라 8/10 실측·채점 API 편당 실측), fal은 참가자 선불이라 회사 부담 0, 11월 전 언제든 낼 수 있음. 본부가 인계 브리프만 보고 확인 없이 "진행 중"으로 보고한 게 원인이었다 — 이 세션이 못 찾은 게 맞았다. **다시 찾지 말 것** |
| c11 | season_1001~1006 리허설 잔여 행 노출 차단 | **조치 불필요.** 실측: 6개 행 전부 is_fixture=true·status=completed·open/close NULL, genesis_applications 0건. `getWatchSeasonGroups()`는 영상이 있는 시즌만 그룹을 만들어(bySeason이 videos에서 파생) 0영상 시즌은 애초에 순회 대상이 아니다. `getSeasonMeta()`(비export, 유일 호출부 lib/watch.ts:990)에 is_fixture 필터를 넣는 안은 **기각** — season_test(is_fixture=true)가 실제 영상을 갖고 있어 이 메타(표시명)를 정당하게 필요로 하는 유일한 호출부이고, 필터를 걸면 그 표시명이 퇴화한다(seasonId로 폴백). 막을 노출 경로가 없는데 유일한 실사용처를 깨는 것은 손해만 있다 |
| c12 | E2E 8+1종 | **✅닫힘 2026-08-03 / `7f22cc0`.** 완료 내역(2026-08-02~08-03): 9번 도달성 7/7(`248e8a6`) · 변조 8종→S2 수정 후 10/10(`3df501e`) · KAT 불변 35/35·requireFinal 6/6·시간압축 마감 12/12·실패→재렌더 20/20·동기 경로 회귀 포함(`7f22cc0`). 본부가 옛 브리프의 "2.0d 남았다" 수치를 확인 없이 잔여로 옮긴 게 원인 — 제니2가 올린 지수2A 대기 5건에도 E2E가 없어 교차 확인됨 |
| c13 | A2P 10DLC SMS(8) — 마이그+코드+배포 전부 완료 | 2026-08-11 실측: `profiles`에 sms_optin 6컬럼 전부 존재 + `SmsConsentCard.tsx`가 이미 `main`에 병합돼 있음(2026-06-23 라이브). 재확인 불필요 |
| c14 | Member Hosted Tournament(9) — GRANT 마이그 완료 | 2026-08-11 실측: `platform_config`/`member_tier_config`/`partner_tournaments` service_role 조회 정상(예전 permission denied 해소) + `profiles.partner_tier` 존재. `/host/new`도 이미 `main` 병합(마스터스위치 기본 OFF, 켜는 건 별개 비즈니스 판단) |
| c15 | award-gate 부분 코호트 확정(5) — 이미 닫혀 있음 | `lib/awards-gate.ts` Gate 2가 `scoredCount < submittedCount`면 사유 불문 무조건 막음(재시도 소진 실패도 포함, 9/10도 막힘을 테스트로 확인). countBlockingFailed(예선용, 다른 코드)보다 엄격한 상위호환이라 별도 배선 불필요 |
| c16 | Winners nav 항목(soon:true) 처리 | **철회 → 유지 확정(2026-08-11 TK).** "코드에 없다=필요 없다"가 아니었음. 설계 승인: 시즌 드롭다운(기본 all, is_fixture 제외) + award_rank 시즌 횡단 + scoring_results(round='main') join 등급 + 카드형. 새 테이블 없음(genesis_applications+scoring_results로 충분). 착수는 발송 콘솔(#17) 완료 후 |
| c17 | `email_optin_migration_2026-08-11.sql` Run 완료 | `profiles`에 email_opt_in/email_consent_at/email_consent_ip/email_consent_text/email_opt_out_at 5컬럼 생성 확인(BLOCK 0=0행 → BLOCK 2=5행 → BLOCK 3 전부 false). 가입 시 동의 UI(`app/login`)+콜백 기록(`app/auth/callback`)+수신거부(`/profile` EmailConsentCard·List-Unsubscribe)까지 코드 배포 완료(`31c63d5`~`e13c878`) |
| c18 | `admin_broadcasts` 테이블 생성 완료 (참가자 연락처 발송 콘솔) | 2026-08-11 Run 확인: tbl 1 / idx 2 / trg 1(updated_at 자동갱신) / rls true / policy 0(service_role 전용, fail-closed). 발송 루프(`lib/email/broadcast-tick.ts` + `/api/cron/broadcast-tick`) 코드 완료, 화면(`/admin/broadcasts`)은 다음 순서 |
| c19 | 배포 대기 13건, `feat/studio-budget-guard`에는 있고 `origin/main`엔 없던 상태 | 2026-08-12/13 HQ 지시로 머지 집행: `feat/studio-budget-guard ← main`(`29382e9`, CI green) → `main ← feat/studio-budget-guard`(`33aa399`, tsc·507/507·`next build` 전부 clean, 충돌 0) → `main` push(`a8b4f27..33aa399`) → CI green → `deploymentEnabled.main=false` 그대로(자동배포 없음, `builtAt` 불변 확인). ★배포 자체는 별개 — lane-c(제니2 트랙, 앱 16·워커 5) 병합 뒤 대표님이 승인 |
| c26 | 26. `logEmail()` DB insert 에러 삼킴 | **2026-08-17 고침, 배포됨(`7f06329`).** `logEmail()`이 성공/실패를 리턴하도록 시그니처 변경, `executeSend`의 'sent' 성공 경로에서 로그 insert가 실패하면 `sendAdminAlert`로 관리자에게 알림(발송 자체의 `ok:true`는 안 바꿈 — 실제로 보냈는데 실패로 리턴하면 다른 각도의 중복발송 위험이 생겨서). tsc clean, 561/561 |
| c37 | **faq_items 마이그가 service_role GRANT 없이 나감 → 발견·복구·자동화.** 2026-08-16~17 감사 | `GRANT ALL ... TO service_role` 누락으로 SELECT/INSERT/UPDATE/DELETE 0개(REFERENCES/TRIGGER/TRUNCATE만 존재) 실측 재현. 같은 패턴으로 `chat_logs`/`email_inbound_log`(2026-06, 생성 시점부터)도 걸려 있던 것 함께 발견 — email_inbound_log는 dedup·일일상한 체크가 `error`를 안 보고 통과시키는 상태였음(아래 c-inbound). 3건 GRANT 복구. `check_service_role_grants()` RPC(SECURITY DEFINER) 신설 + `scripts/check-service-role-grants.mjs` + `deploy:prod` 하드 게이트로 자동화 — 이후 실측: `applications`/`backup_genesis_user_id_20260606` 2건 추가 결손 발견(레거시 판정, grant는 해줌 — 예외 목록 대신 임계 0행 유지가 TK 판정). 스크립트 자체에 `process.exit()` 강제종료가 Windows에서 크래시해 통과했는데도 배포를 막는 2차 버그도 발견·수정(`0d1640d`, `process.exitCode`로 교체) |
| c-inbound | email_inbound_log dedup·일일상한이 `error`를 안 보고 통과 | **2026-08-16 고침, 배포됨(`7f06329`).** `app/api/email/inbound/route.ts` — 쿼리 실패 시 `data`/`count`만 읽고 `error`는 버려서 "중복 아님"/"상한 밑"으로 기본 통과하던 것을 fail-closed로 교체(에러면 무조건 `skipped/db_error`, 발신 안 함). 같은 감사에서 `oxxovo-studio`의 `probeDurationSec`(ffprobe 실패 시 0초로 폴백)도 같은 병으로 판정해 fail-closed 처리(`e17b139`) |
| c-leak | **`main_round_theme_label`이 `seasons_public` 뷰로 anon에 직접 유출** — 리빌 게이트와 무관하게 REST로 바로 읽힘(재현 완료), `/profile` MainRoundCard도 같은 anon 소스를 게이트 없이 렌더 | **2026-08-16~17 순서대로 처리.** ①Watch(`resolveSeasonStage`)·`/profile`을 서비스롤 경유 `getRevealedTheme()`로 리포인팅(`7f06329`, 배포 먼저) ②그다음 `main_round_theme`(트위스트 문장이 통째로 든 구버전 브리프 컬럼, `main_round_theme_label`과 다른 컬럼)을 뷰에서 `DROP VIEW`+`CREATE VIEW`(정확한 순서는 REPLACE로 두 번 시도했다가 컬럼 삭제/재배치는 REPLACE가 못 한다는 걸 배워서 대표님이 `pg_get_viewdef` 실측 원문 주심, 그걸로 최소 diff) — Run 완료, 검증 68컬럼·`main_round_theme` 0행 확인 |
| c-gate | **주제·트위스트 게이트 재설계 — 라벨은 무게이트, 트위스트만 시각 게이트** | **2026-08-17 설계+배포(`8724c7d`).** 8/13(`37c7f8f`) git 고고학: 그 커밋 전엔 라벨이 아예 안 그려졌었다(게이트가 있었던 게 아니라 기능 자체가 없었음) — "되돌리기"가 아니라 신규. `ThemeDisplay`를 `prelimTheme`/`mainTheme`(무게이트) + `twist`/`twistRevealed`(`isTwistRevealed`, 11/8 19:00 PT)로 분리, 폴백 체인 제거(라벨이 있으면 예선 "자유 주제" placeholder를 즉시 밀어내던 실제 버그였음). Studio `ThemeBanner` 두 줄 분리. `getRevealedTheme()`가 유일한 읽기 지점이라는 규칙 주석에 명시. tsc clean, 561/561 |
| c-faqdeploy | FAQ 편집기 실배포 + 실측 | **2026-08-17.** `npm run deploy:prod`(우회 없이, 게이트 통과) → `www.oxxovo.ai/api/version` 라이브 일치. CRUD 5스텝 전부 서비스롤/anon 대조로 재현: 생성·`is_active=false` 비공개/`true` 공개 토글 양방향·`sort_order` 순서 반영·수정·삭제, 전부 통과. 금지어 가드는 `platform_config` 키 0개로 미가동 상태 확인 |
| c38 | Seedance 2.5 등록 | **2026-08-17.** `fal_model_id="bytedance/seedance-2.5/text-to-video"` fal 공식 API 문서 원문 확인(추정 아님). 단가 720p $0.4730/s(fal 모델 페이지 원문). 실호출 1회(720p·10초·오디오) 성공 — ffprobe 실측 1280x720/10.08s, 프레임 캡처 육안 확인 양호. `model_catalog`에 `seedance2-5`(premium, sort_order 9) 추가, `seedance2`(2.0)는 유지 |
| c36 | **다음 창 첫 답 3건** — ①generate_audio ②sort_order 9 ③/faq | **2026-08-17 조사 완료.** ①기존 4개 모델(veo3.1/veo31-lite/kling-v3-pro/seedance2)이 이미 `generate_audio:true` — seedance2-5는 기존 패턴을 따른 것, 새 문제 아님. 웹서치로 MPA/Netflix 등 Seedance 분쟁을 찾았으나 **2026-02 사건, Seedance 2.0 대상, MPA-ByteDance 합의 완료·2.5는 그 대응판**(HQ 정정) — 진짜 방어선은 저작권 캐릭터/실존인물 금지 조항 부재였고 항목 #41로 분리. ②`sort_order=9`는 비활성 포함 전수(20행)에서 유일값 확인, 실제 중복은 `sort_order=1`(항목 #40로 분리). ③`/faq`(`app/faq/page.tsx`)는 `faq_items` 편집기 미연결(그 시스템은 `landing_home` surface 전용), ko 경로 없음(영어 고정), Q1·Q4 "외부 툴 자유" 둘 다 거짓 확정(`session6_enabled=true`+`season_0.studio_round='both'`로 실측, `/apply`가 외부 URL 완전 차단). 고치려면 코드 수정+배포 필요. 문안은 제니3 대기 |
| c-ipcheck | **프롬프트 IP/초상 사전검사 — 설계·구현·배포·실측 완료.** `/studio` 3개 생성 진입점(text-to-video·i2v·image) 전부 배선 | **2026-08-17.** `lib/ip-check.ts`(`d793446`) — Claude Haiku, 6초 타임아웃, fail-open(에러·타임아웃·키누락 전부 차단 안 함), 임계값 `platform_config.ip_check_block_confidence`(기본 'high', 코드 하드코딩 아님), `generation_jobs.ip_check_status/note` 기록. 마이그(`reports/ip_check_migration_2026-08-17.sql`) Run 완료 후 `npm run deploy:prod`(우회 없음) 배포. `scripts/verify-ip-check.mjs`(`6bb802b`) — season_test 실계정으로 fail-open·명백한 IP 차단(과금 0 확인)·DB 상태 기록 3종 실측, 19/19 통과. HQ 요청으로 borderline(flagged, 비차단) 경로 1건 추가 실측도 완료. 실측 중 발견: 실제 모델이 confidence를 binary(high/미차단)로만 내고 medium/low는 관측된 프롬프트 범위 내에서 나오지 않음 — 정직하게 그대로 보고, 결과 조작 안 함 |
| c-champpoints | **Championship Points 적립 시스템 — 설계·구현·배포·회귀 확인 완료.** 3개 훅(참가/톱50/시상) 전부 원장 기반 | **2026-08-18.** `lib/championship-points.ts`(`771df32`) — reversal/re-credit 체인(재지급의 `reverses_id`는 원본이 아니라 리버설을 가리켜 partial unique index와 영구 충돌 안 함), 헤드카운트·배수·기준액은 참가 시점에 1회 고정(`getFrozenBasis`), `basis_usd IS NULL`이면 fail-closed+`sendAdminAlert`. season-tick(`app/api/cron/season-tick/route.ts`)에 참가(1.7단계)·톱50(어드밴스 직후) 배선, 시상 3개 admin 액션(`saveAwardRank`/`approveTop3Awards`/`saveAwardOverride`)에 `syncAwardPoints` 배선. 마이그(`championship_points_migration_2026-08-18.sql`, 브래킷 0~999999999로 코드 클램프 없이 데이터로 표현) Run 완료 후 정식 배포. `scripts/verify-championship-points.mjs` 24/24 통과(season_test 41개 기존 유효행 발견 후 원장 절대수 대신 개별 신청 ID 기준으로 검증 재작성) + 로컬 dev로 실제 프로덕션 DB 대상 `/api/cron/season-tick` 회귀 실행 `ok:true, errors:[]`(기존 리마인더/예선홀드/본선 메일 경로 무영향 확인) |
| c-watchsidebar | **Watch 사이드바 CREATOR RANKING 박스 + `/watch/rankings` 정보 페이지 — 3차 재설계 끝에 프로덕션 배포.** | **2026-08-18, `ec0dc47`.** 1차(그리드 상단 가로 배치) HQ 반려(그리드가 첫 화면 아래로 밀림) → 2차(좌측 사이드바 WATCH 배지 자리로 이전, 긴 이름 잘림 처리는 2026-08-06 헤더 사고와 같은 패턴으로 방지: `min-w-0 flex-1 truncate`+`shrink-0`) → 3차(깜박이는 문구·모션 전면 삭제, 박스는 줄어든 대로) 순서로 HQ 지시에 맞춰 재설계, 매 회 Preview로 확인 후 승인. "전체 랭킹 보기"는 `/watch/rankings`(정보 페이지, 새 창)로 실제 연결 — 실순위 계산·500등 목록·개별 심사평 화면은 2027년 3월 전으로 명시적 범위 밖. 문안 3칸은 제니3 대기(지어내지 않음, HQ 표현을 라벨로만 사용), 공개시점(`championship_points_reveal_at`, 잠정값)·빈줄수(`championship_points_placeholder_rows`)는 설정값. 배포는 `npm run deploy:prod` 정식 경로. `/watch` 자체가 발사 전 게이트(`isWatchPublic()`)로 프로덕션에서 닫혀 있어(오늘 작업과 무관, 기존 상태) 라이브 URL 육안 검증 불가 — 프로덕션 DB에 연결한 로컬 dev 서버로 마크업 기준 5개 항목(박스 표시·그리드 안 밀림·모바일 최상단·새 창·무모션) 전부 실측 확인. tsc clean, 561/561. 남은 4건은 #42~#45로 분리 |
| c-alertstorm | eb0ba95 admin-alert 폭주 진화 배포 | 2026-08-19 배포 확인(`59bf30e`). is_fixture 시즌 알림 제외 + sendAdminAlertOnceDaily 하루1회 캡, season-tick 3곳 배선 |
| c-faqrules | 19c37ee FAQ Q1/Q4 허위교체 + `/rules` 저작권 실격조항 배포 | 2026-08-19 배포 확인(`59bf30e`) |
| c-cosmeticguard | b2670a9 화장품 축/조합 프롬프트 가드 배포 + platform_config 5키 Run | 2026-08-19 배포+검증 완료(`59bf30e`, 14/14 pass) |
| c-launch | COMING SOON 게이트 코드 삭제 + Watch=홈 전환 + SITE_PUBLIC_ENABLED 명시 | 2026-08-19 배포 확인(`59bf30e`=로컬HEAD 일치). oxxovo.ai가 Watch로 뜸, X-Robots-Tag 제거, `/coming-soon` 404 확인 |
| c-waitlistpromise | `/faq` Q5 + `/apply` 2곳 + 랜딩FAQ(나이제한/구독료/대기자승격 3건) 허위-약속 문구 제거 | 2026-08-19 배포 확인(`59bf30e`). 미구현 기능(대기자 자동승격) 약속 전부 제거, 동일 문장 재사용으로 정본 통일 |
| c-nicknamegate | **닉네임 온보딩 게이트 — 자동생성 제거+콜백 리다이렉트+mint 백스톱+온보딩 화면+3규칙 금지어 엔진(단어경계/포함/leetspeak정규화)+정규화 unique 인덱스, 전부 배포·검증 완료** | 2026-08-19 배포 확인(`6591c85`=prod sha 일치, `merge-base --is-ancestor` 확인). tsc clean·564/564. 라이브 금지어 실측 4/4("시발"거부·"안성재"통과·"OXXOVO_KIRA"거부·"0xx0v0"거부), collision-key SQL/JS 패리티 13/13. ★브라우저로 실제 화면(리다이렉트 탈출 불가·중복거부·실명토글 경고·제출 후 이동)까지는 미확인 — 다음 세션 첫 줄 후보 |
| c-bannedseed | 닉네임 금지어 `platform_config` 시딩 — general 68 · impersonation 37(장애인 제외 확정) | 2026-08-19 Run 완료 |
| c-creatorxxxx | CreatorXXXX 자동생성 닉네임 3행(TK 계정 포함) `display_name` NULL 처리 | 2026-08-19 Run 완료, BLOCK 0 미리보기 일치 확인 후 BLOCK 1 실행 |
| c-watermarkcols | 다운로드/홍보 워터마크용 컬럼 마이그(profiles 5 + render_jobs 5+2) | 2026-08-19 Run 완료 전부 |
| c-submitgatehardening | **발사 후.** `submitGenerationAction`/`submitRenderAction`(`app/studio/actions.ts`)이 로그인 토큰만 확인하고 `checkStudioAccess`를 직접 부르지 않음 — 2026-08-27 추적 결과 일반 참가자에겐 도달 불가(job/render를 만드는 모든 create* 경로가 이미 checkStudioAccess로 막힘), admin/`STUDIO_DEV_UNLOCK`/`studio_test_access` 우회 계정에만 열려 있음(공개 구멍 아님). `submitGeneration`/`submitRender`(lib/studio.ts) 초입에 `checkStudioAccess` 방어적 재호출 추가 — TK 승인된 원칙(이중검증≠이중진실, 같은 함수 재호출은 방어이지 값 중복 아님)이지만 9/9 발사 17일 전에 제출 경로를 건드리는 위험이 커서 지금은 보류 | open |
| c-aspectadminui | `seasons.aspect_ratio`를 어드민 폼(`SeasonForm.tsx`)에서 못 바꾼다 — SQL로만 설정 가능. TK 원칙("모든 시즌값은 어드민에서 조정 가능해야 함")과 어긋남. 라디오(16:9/9:16/자유선택 3택, NULL을 표현하려면 "자유선택"을 명시 옵션으로) + `season-schema.ts` 검증 추가 필요. 2026-08-27 A 배포 시 TK 승인으로 보류 | open |
| c-seasonspublic-nowhere | `seasons_public` 뷰에 `WHERE` 절이 없다 — 컬럼만 제한되고 행은 전부(season_1000~1006 픽스처 포함) anon에 노출됨. 필터링은 앱 레이어(`isFixtureSeason()`)에만 있음, DB 뷰 자체는 안 거름. 2026-08-27 뷰 정의 원문 확인 중 발견. 오늘 새로 생긴 문제 아님 — 원래부터 이랬음. 의도된 설계(앱 레이어 필터링)인지 놓친 것인지 TK 판단 필요 | open |
| c-sharedworkfolder | **작업 폴더(`C:\Users\Tom\oxxovo`)를 여러 세션이 동시에 공유한다 — 배포마다 재발하는 구조적 위험.** 2026-08-27 확인: 세션 시작 시점에 이미 다른 세션들이 남긴 미커밋 파일 15개(`outputs/` 10개 + `reports/` 7 + `scripts/zz_probe_*` 7, `public/` 밑은 없어 유출 위험은 없었음)가 떠 있었다. `deploy-prod.mjs`는 untracked 파일이 하나라도 있으면 dirty로 거부(파괴 위험 없는 안전장치 자체는 정상 동작) — 배포 세션이 매번 그 파일들을 `git stash push -u`로 치웠다가 배포 직후 `git stash pop`으로 복원하는 수작업을 반복해야 했다(이번 세션만 2회). **2026-08-26 저녁, 한 세션 안에서 서로 무관해 보이는 트랙(studio 나이게이트·apply 필드·lobby 리팩터·tournament/rules 카피·admin FAQ 토큰) 커밋 24개가 21:09~23:37 사이에 뒤섞여 났다** — 어느 게 어느 세션(지수/지수2/지수2C 등) 소속인지 커밋 로그만으로는 안 갈린다("정체 모를 커밋", HQ 2026-08-27 지적). `git stash`로 매번 치우고 되돌리는 지금 방식은 그 자체로 사고 가능성을 갖고 있다(pop 실패 시 다른 세션 작업물 유실) — 이번엔 15개 전부 무사히 복원됐지만 구조적으로 안전한 방식이 아니다. **HQ 방침: 발사 후 세션별 작업 폴더(git worktree 또는 별도 clone)로 분리한다.** 지금은 관측·기록만, 조치 없음 | open |
